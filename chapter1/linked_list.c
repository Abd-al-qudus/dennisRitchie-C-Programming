#include <stdio.h>
#include <unistd.h>

typedef struct student_t{
    char *name;
    char *DOB;
    size_t level;
}student_s;

student_s add_node(student_s **head){
    student_s new;
    char *stdName, *stdDOB;
    ssize_t

    stdName = malloc(sizeof(char) * 20);
    if (stdName == NULL)
        return (NULL);
    new = malloc(sizeof(student_s));
    if (new == NULL)
        return (NULL;


}

int main(int ac, char **av){
    student_s stud1;

    stud1 = NULL;

}
